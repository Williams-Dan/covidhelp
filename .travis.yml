language: ruby

rvm:
  - 2.7.0

before_install:
  - gem install bundler -v 2.0.2
addons:
  apt:
    update: true
  
after_success:
### Once built - check if service is available (unsure of port right now)
  - curl http://127.0.0.1:8000

script:
  - rake db:migrate
  - rake test
  - rake run
